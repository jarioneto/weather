import {
  borderWidths,
  colors,
  fonts,
  fontSizes,
  fontWeights,
  letterSpacings,
  lineHeights,
  radii,
  sizes
} from '../tokens';

export const weather = {
  colors,
  fonts: {
    heading: `Poppins, ${fonts.heading}`,
    body: `Poppins, ${fonts.body}`
  },
  sizes,
  space: sizes,
  fontSizes,
  letterSpacings,
  lineHeights,
  fontWeights,
  borderWidths,
  radii
};
